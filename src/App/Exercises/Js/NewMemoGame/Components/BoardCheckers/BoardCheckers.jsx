export const checkIstwo = (difficulty) => {
  return Boolean(difficulty === 2);
};
export const checkIsFour = (difficulty) => {
  return Boolean(difficulty === 4);
};
export const checkIsSix = (difficulty) => {
  return Boolean(difficulty === 6);
};
